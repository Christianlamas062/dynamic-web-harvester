services:
  harvester:
    build: .
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data/processed:/app/data/processed
    command: ["--max-pages", "1", "--format", "csv", "--output-dir", "data/processed"]
